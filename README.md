Sources for the plugin `plugin.quickAction`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.quickAction" = {
            publisherId = "tech.scotth",
        },
    },
}
```
